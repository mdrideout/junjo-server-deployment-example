# Environment variables for Junjo Server
# Copy this file to .env and fill in the values.

# Environment: development || production
JUNJO_ENV="development"

# Production Authentication Domain
# This is the production top level (root) domain that will be used for session cookie authentication
# This will automatically cover all subdomains
# This is only used when ENV="production"
# 
# Example:
# JUNJO_PROD_AUTH_DOMAIN="example.com"
# COVERS:
#   - example.com
#   - www.example.com
#   - api.example.com
JUNJO_PROD_AUTH_DOMAIN="example.com"

# A secret key used for signing sessions.
# You can generate a secure key with: openssl rand -base64 48
JUNJO_SESSION_SECRET="your_super_secret_key"

# The allowed origins for CORS (comma-separated)
# For local development, this is usually the frontend's address.
# For production, include the frontend's domain.
# Example: ALLOW_ORIGINS=http://localhost:5151,http://localhost:5153,https://app.yourdomain.com
JUNJO_ALLOW_ORIGINS=http://localhost:5151,http://localhost:5153

# The Junjo Server API key.
# This is used to authenticate requests to the Junjo Server API.
# You can create an API key in the Junjo Server UI.
JUNJO_SERVER_API_KEY="your_api_key"

